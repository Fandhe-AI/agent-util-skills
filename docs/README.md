# docs/

本ディレクトリはリポジトリの運用メモ置き場である。`skills/` のように `npx skills add` で
導入先へ配布される対象ではなく、本リポジトリを管理する側だけが参照する。

## 索引

| ファイル | 概要 |
|---------|------|
| [skill-network-requirements.md](skill-network-requirements.md) | `skills/` 配下の自前スキルのネットワーク・sandbox 実行要件 |

## ガード検証

本節は setup-repo-guards 導入時の CI ガード検証 PR で追加した（codex-review・必須チェック報告・squash merge 限定・スレッド解決必須の実測確認）。
